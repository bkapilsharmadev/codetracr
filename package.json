{
  "name": "codetracr",
  "version": "0.1.0",
  "private": true,
  "description": "Code lineage explorer for change-impact analysis",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build -w @codetracr/web",
    "start": "node --experimental-strip-types packages/server/src/server.ts",
    "ui": "npm run build && npm start",
    "poc": "npm run poc -w @codetracr/engine",
    "generate": "npm run generate -w @codetracr/engine",
    "analyze": "node --experimental-strip-types packages/engine/src/analyze.ts",
    "test": "npm run generate && npm run test -w @codetracr/engine && node --experimental-strip-types --test packages/server/test/*.test.ts",
    "typecheck": "npm run typecheck -w @codetracr/engine && npm run typecheck -w @codetracr/server"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.20.1",
    "typescript": "^5.7.3"
  }
}
